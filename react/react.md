# React 学习笔记

## 创建项目

```bash
npx create-react-app my-app --template typescript
```

- `my-app` — 项目名称
- `--template typescript` — 使用 TypeScript 模板

## 运行项目

```bash
cd my-app
npm start
```

## 修改端口

1. 在项目根目录下创建 `.env` 文件
2. 在 `.env` 文件中添加：
```env
PORT=3001
```
